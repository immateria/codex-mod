// Render helpers live alongside their mode-specific implementations in
// `main.rs`, `editor.rs`, and `picker.rs` for now. This module exists as the
// consolidation point for later render-only cleanup.
